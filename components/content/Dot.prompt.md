Use `Dot` wherever another system would use a bullet, a step marker or a status pip.

```jsx
<Dot />
<Dot pigment="var(--pigment-water)" size={12} />
```

- The full circle is the only curve in the brand. Everything else is square.
